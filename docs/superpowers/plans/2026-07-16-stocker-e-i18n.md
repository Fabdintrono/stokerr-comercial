# Stocker E: i18n + auto-traducción — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Unificar el i18n de Stocker en un solo sistema key-based (`t('clave')`) con 3 idiomas (es base / pt / en), traducciones auto-generadas por script en build, y extraer las pantallas comerciales a `t()`.

**Architecture:** Un único `I18nProvider` (sobre `src/lib/i18n.tsx`) carga 3 JSON de `locales/`, expone `{ locale, setLocale, t }` con fallback `locale → es → key` e interpolación `{var}`. El PDF (server-side) usa `tServer(locale, key)` que lee los mismos JSON. Un script Node (`scripts/i18n/translate.mjs`) rellena claves faltantes en pt/en vía un proveedor aislado. Se eliminan los sistemas i18n muertos (`src/lib/i18n/`, `src/locales/`).

**Tech Stack:** Next.js 16 App Router, React client provider, Prisma (`User.language`), Vitest (node env), `Intl` para fechas/números, ESM script Node (`.mjs`).

---

## File Structure

**i18n core (client + server):**
- `src/lib/i18n.tsx` — MODIFY: provider reescrito (es/pt/en, 3 JSON, fallback a es, persistencia PATCH preferences).
- `src/lib/i18n/tServer.ts` — CREATE: `tServer(locale, key, vars?)` para render server (PDF). (Nota: la carpeta `src/lib/i18n/` se vacía de los archivos muertos y solo queda `tServer.ts`.)
- `src/lib/i18n/normalizeLocale.ts` — CREATE: `normalizeLocale(raw): 'es'|'pt'|'en'` (mapea legacy `pt-PT`/`es-ES`/`en-GB`).
- `src/lib/i18n/format.ts` — CREATE: `formatDate(date, locale)`, `formatNumber(n, locale)` con `Intl`.

**Locales:**
- `locales/es.json` — MODIFY: fuente de verdad, agregar namespaces comerciales.
- `locales/pt.json` — MODIFY: paridad con es (traducciones pt).
- `locales/en.json` — CREATE: paridad con es (traducciones en).

**Script de traducción:**
- `scripts/i18n/mergeMissing.mjs` — CREATE: función pura `mergeMissing(base, target)`.
- `scripts/i18n/provider.mjs` — CREATE: proveedor aislado (LLM/DeepL vía `TRANSLATION_API_KEY`).
- `scripts/i18n/translate.mjs` — CREATE: CLI que orquesta lectura/merge/escritura.
- `scripts/i18n/README.md` — CREATE: cómo correr el script.

**Tests:**
- `src/lib/i18n/t.test.ts` — CREATE: `t()` (existente, interpolación, fallback a es, fallback a key).
- `src/lib/i18n/parity.test.ts` — CREATE: paridad de claves es/pt/en.
- `src/lib/i18n/normalizeLocale.test.ts` — CREATE.
- `scripts/i18n/mergeMissing.test.mjs` — CREATE: probado vía vitest (se agrega glob).

**Schema + API:**
- `prisma/schema.prisma:88` — MODIFY: `User.language` default `"es"`.
- `src/app/api/users/preferences/route.ts` — MODIFY: enum a `['es','pt','en']` + normaliza legacy.

**Pantallas comerciales (extracción a `t()`):**
- `src/components/layout/LanguageSelector.tsx` — MODIFY: usa `useI18n().setLocale`, idiomas es/pt/en.
- `src/app/providers.tsx` — MODIFY: quitar `defaultLocale="pt"`.
- `src/app/billing/page.tsx`, `src/components/billing/SubscriptionGate.tsx`
- `src/app/warehouse/settings/currency/page.tsx`, `src/app/warehouse/settings/rates/page.tsx`
- `src/app/super-admin/modules/page.tsx`, `src/components/modules/ModuleGate.tsx`, sección módulos en licenses
- `src/app/warehouse/customers/page.tsx`, `src/app/sales/new/page.tsx`, `src/components/pdf/SaleDocumentPdf.tsx`
- `src/app/warehouse/settings/page.tsx` (branding/impuesto)
- `src/app/(auth)/login/page.tsx`

---

## Convención de claves (namespaces)

Todas las claves nuevas se agregan **primero a `locales/es.json`** (español real). Namespaces por punto:

```
common.*      save, cancel, loading, delete, edit, create, search, close, back, confirm
auth.*        login, email, password, ...
billing.*     title, payUsdt, statusActive, statusGrace, statusExpired, renewBanner, ...
currency.*    title, base, secondary, save, ...
rates.*       title, manual, provider, ...
modules.*     notActive, catalog, addon, ...
invoicing.*   newInvoice, customer, subtotal, tax, total, document, ...
settings.*    branding, taxLabel, taxRate, prefix, ...
```

Regla: **nunca** usar `.` dentro del segmento final como clave literal — cada punto es un nivel de anidamiento. (P.ej. usar `billing.statusActive`, NO `billing."status.active"`.)

---

## Task 1: `normalizeLocale` helper (legacy → es/pt/en)

**Files:**
- Create: `src/lib/i18n/normalizeLocale.ts`
- Test: `src/lib/i18n/normalizeLocale.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/i18n/normalizeLocale.test.ts
import { describe, it, expect } from 'vitest'
import { normalizeLocale, LOCALES } from './normalizeLocale'

describe('normalizeLocale', () => {
  it('maps legacy region codes to short codes', () => {
    expect(normalizeLocale('pt-PT')).toBe('pt')
    expect(normalizeLocale('es-ES')).toBe('es')
    expect(normalizeLocale('en-GB')).toBe('en')
  })
  it('passes through valid short codes', () => {
    expect(normalizeLocale('es')).toBe('es')
    expect(normalizeLocale('pt')).toBe('pt')
    expect(normalizeLocale('en')).toBe('en')
  })
  it('falls back to es for unknown / empty / null', () => {
    expect(normalizeLocale('fr')).toBe('es')
    expect(normalizeLocale('')).toBe('es')
    expect(normalizeLocale(null)).toBe('es')
    expect(normalizeLocale(undefined)).toBe('es')
  })
  it('exposes the supported locale list', () => {
    expect(LOCALES).toEqual(['es', 'pt', 'en'])
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun run test -- src/lib/i18n/normalizeLocale.test.ts`
Expected: FAIL — cannot find module `./normalizeLocale`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/i18n/normalizeLocale.ts
export type Locale = 'es' | 'pt' | 'en'
export const LOCALES: Locale[] = ['es', 'pt', 'en']
export const DEFAULT_LOCALE: Locale = 'es'

export function normalizeLocale(raw: string | null | undefined): Locale {
  if (!raw) return DEFAULT_LOCALE
  const short = raw.slice(0, 2).toLowerCase()
  return (LOCALES as string[]).includes(short) ? (short as Locale) : DEFAULT_LOCALE
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun run test -- src/lib/i18n/normalizeLocale.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n/normalizeLocale.ts src/lib/i18n/normalizeLocale.test.ts
git commit -m "feat(i18n): normalizeLocale helper (legacy → es/pt/en)"
```

---

## Task 2: Delete dead i18n systems

**Files:**
- Delete: `src/lib/i18n/translations.ts`, `src/lib/i18n/useTranslation.ts`
- Delete: `src/locales/es.json`, `src/locales/pt.json` (carpeta `src/locales/`)

- [ ] **Step 1: Confirm nothing imports the dead code**

Run: `grep -rn "lib/i18n/useTranslation\|lib/i18n/translations\|from ['\"].*src/locales\|@/locales\|useTranslation" src --include=*.ts --include=*.tsx`
Expected: **no matches** (solo se auto-importaban). Si hay matches, migrarlos a `useI18n` antes de borrar.

- [ ] **Step 2: Delete the files**

```bash
git rm src/lib/i18n/translations.ts src/lib/i18n/useTranslation.ts
git rm src/locales/es.json src/locales/pt.json
```

- [ ] **Step 3: Verify build still typechecks**

Run: `bun run build`
Expected: build OK (sin referencias rotas).

- [ ] **Step 4: Commit**

```bash
git add -A src/lib/i18n src/locales
git commit -m "chore(i18n): remove dead translation systems (src/lib/i18n/*, src/locales/)"
```

---

## Task 3: Locale files with commercial namespaces (es source + pt/en parity)

**Files:**
- Modify: `locales/es.json`
- Modify: `locales/pt.json`
- Create: `locales/en.json`

- [ ] **Step 1: Write `locales/es.json` (source of truth)**

Reemplaza el contenido completo por (conserva namespaces existentes `common/auth/nav/dashboard/roles` y agrega los comerciales):

```json
{
  "common": {
    "appName": "Stocker",
    "loading": "Cargando…",
    "save": "Guardar",
    "cancel": "Cancelar",
    "delete": "Eliminar",
    "edit": "Editar",
    "create": "Crear",
    "close": "Cerrar",
    "back": "Volver",
    "confirm": "Confirmar",
    "search": "Buscar",
    "filter": "Filtrar",
    "actions": "Acciones",
    "status": "Estado",
    "date": "Fecha",
    "name": "Nombre",
    "description": "Descripción"
  },
  "auth": {
    "login": "Iniciar sesión",
    "logout": "Cerrar sesión",
    "email": "Correo electrónico",
    "password": "Contraseña",
    "loginTitle": "Inicia sesión en tu cuenta",
    "loginError": "Credenciales inválidas",
    "language": "Idioma"
  },
  "billing": {
    "title": "Mi suscripción",
    "plan": "Plan",
    "amount": "Monto mensual",
    "nextPayment": "Próximo pago",
    "payUsdt": "Pagar en USDT",
    "statusActive": "Activa",
    "statusGrace": "En período de gracia",
    "statusExpired": "Vencida",
    "graceBanner": "Tu suscripción está en período de gracia. Renueva antes de {date}.",
    "expiredBanner": "Tu suscripción venció. Renueva para seguir usando Stocker."
  },
  "currency": {
    "title": "Monedas",
    "base": "Moneda base",
    "secondary": "Moneda secundaria",
    "none": "Ninguna",
    "saved": "Monedas actualizadas"
  },
  "rates": {
    "title": "Tasas de cambio",
    "manual": "Manual",
    "source": "Fuente",
    "value": "Valor",
    "updated": "Tasa actualizada"
  },
  "modules": {
    "notActive": "Módulo no activo",
    "notActiveDesc": "Este módulo no está incluido en tu plan. Contáctanos para activarlo.",
    "catalog": "Catálogo de módulos",
    "addon": "Complemento"
  },
  "invoicing": {
    "newInvoice": "Nueva factura",
    "customer": "Cliente",
    "customers": "Clientes",
    "document": "Comprobante",
    "quantity": "Cant.",
    "unitPrice": "Precio",
    "subtotal": "Subtotal",
    "tax": "Impuesto",
    "total": "Total",
    "voucher": "COMPROBANTE"
  },
  "settings": {
    "branding": "Marca",
    "logo": "Logo",
    "taxEnabled": "Cobrar impuesto",
    "taxLabel": "Etiqueta del impuesto",
    "taxRate": "Tasa del impuesto (%)",
    "docPrefix": "Prefijo del comprobante",
    "saved": "Ajustes guardados"
  }
}
```

- [ ] **Step 2: Write `locales/pt.json` (mismo set de claves, portugués)**

```json
{
  "common": {
    "appName": "Stocker",
    "loading": "Carregando…",
    "save": "Guardar",
    "cancel": "Cancelar",
    "delete": "Eliminar",
    "edit": "Editar",
    "create": "Criar",
    "close": "Fechar",
    "back": "Voltar",
    "confirm": "Confirmar",
    "search": "Pesquisar",
    "filter": "Filtrar",
    "actions": "Ações",
    "status": "Estado",
    "date": "Data",
    "name": "Nome",
    "description": "Descrição"
  },
  "auth": {
    "login": "Entrar",
    "logout": "Sair",
    "email": "E-mail",
    "password": "Senha",
    "loginTitle": "Entre na sua conta",
    "loginError": "Credenciais inválidas",
    "language": "Idioma"
  },
  "billing": {
    "title": "Minha assinatura",
    "plan": "Plano",
    "amount": "Valor mensal",
    "nextPayment": "Próximo pagamento",
    "payUsdt": "Pagar em USDT",
    "statusActive": "Ativa",
    "statusGrace": "Em período de carência",
    "statusExpired": "Expirada",
    "graceBanner": "Sua assinatura está em período de carência. Renove antes de {date}.",
    "expiredBanner": "Sua assinatura expirou. Renove para continuar usando o Stocker."
  },
  "currency": {
    "title": "Moedas",
    "base": "Moeda base",
    "secondary": "Moeda secundária",
    "none": "Nenhuma",
    "saved": "Moedas atualizadas"
  },
  "rates": {
    "title": "Taxas de câmbio",
    "manual": "Manual",
    "source": "Fonte",
    "value": "Valor",
    "updated": "Taxa atualizada"
  },
  "modules": {
    "notActive": "Módulo não ativo",
    "notActiveDesc": "Este módulo não está incluído no seu plano. Fale conosco para ativá-lo.",
    "catalog": "Catálogo de módulos",
    "addon": "Complemento"
  },
  "invoicing": {
    "newInvoice": "Nova fatura",
    "customer": "Cliente",
    "customers": "Clientes",
    "document": "Comprovante",
    "quantity": "Qtd.",
    "unitPrice": "Preço",
    "subtotal": "Subtotal",
    "tax": "Imposto",
    "total": "Total",
    "voucher": "COMPROVANTE"
  },
  "settings": {
    "branding": "Marca",
    "logo": "Logo",
    "taxEnabled": "Cobrar imposto",
    "taxLabel": "Rótulo do imposto",
    "taxRate": "Taxa do imposto (%)",
    "docPrefix": "Prefixo do comprovante",
    "saved": "Configurações guardadas"
  }
}
```

- [ ] **Step 3: Write `locales/en.json` (mismo set de claves, inglés)**

```json
{
  "common": {
    "appName": "Stocker",
    "loading": "Loading…",
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "create": "Create",
    "close": "Close",
    "back": "Back",
    "confirm": "Confirm",
    "search": "Search",
    "filter": "Filter",
    "actions": "Actions",
    "status": "Status",
    "date": "Date",
    "name": "Name",
    "description": "Description"
  },
  "auth": {
    "login": "Log in",
    "logout": "Log out",
    "email": "Email",
    "password": "Password",
    "loginTitle": "Log in to your account",
    "loginError": "Invalid credentials",
    "language": "Language"
  },
  "billing": {
    "title": "My subscription",
    "plan": "Plan",
    "amount": "Monthly amount",
    "nextPayment": "Next payment",
    "payUsdt": "Pay with USDT",
    "statusActive": "Active",
    "statusGrace": "In grace period",
    "statusExpired": "Expired",
    "graceBanner": "Your subscription is in its grace period. Renew before {date}.",
    "expiredBanner": "Your subscription has expired. Renew to keep using Stocker."
  },
  "currency": {
    "title": "Currencies",
    "base": "Base currency",
    "secondary": "Secondary currency",
    "none": "None",
    "saved": "Currencies updated"
  },
  "rates": {
    "title": "Exchange rates",
    "manual": "Manual",
    "source": "Source",
    "value": "Value",
    "updated": "Rate updated"
  },
  "modules": {
    "notActive": "Module not active",
    "notActiveDesc": "This module is not included in your plan. Contact us to enable it.",
    "catalog": "Module catalog",
    "addon": "Add-on"
  },
  "invoicing": {
    "newInvoice": "New invoice",
    "customer": "Customer",
    "customers": "Customers",
    "document": "Receipt",
    "quantity": "Qty",
    "unitPrice": "Price",
    "subtotal": "Subtotal",
    "tax": "Tax",
    "total": "Total",
    "voucher": "RECEIPT"
  },
  "settings": {
    "branding": "Branding",
    "logo": "Logo",
    "taxEnabled": "Charge tax",
    "taxLabel": "Tax label",
    "taxRate": "Tax rate (%)",
    "docPrefix": "Receipt prefix",
    "saved": "Settings saved"
  }
}
```

- [ ] **Step 4: Verify JSON validity**

Run: `node -e "['es','pt','en'].forEach(l=>JSON.parse(require('fs').readFileSync('locales/'+l+'.json','utf8')))" && echo OK`
Expected: `OK`.

- [ ] **Step 5: Commit**

```bash
git add locales/es.json locales/pt.json locales/en.json
git commit -m "feat(i18n): es source + pt/en parity with commercial namespaces"
```

---

## Task 4: Key parity test (es == pt == en)

**Files:**
- Create: `src/lib/i18n/parity.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/i18n/parity.test.ts
import { describe, it, expect } from 'vitest'
import es from '../../../locales/es.json'
import pt from '../../../locales/pt.json'
import en from '../../../locales/en.json'

function flatKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  return Object.entries(obj).flatMap(([k, v]) => {
    const key = prefix ? `${prefix}.${k}` : k
    return v && typeof v === 'object' ? flatKeys(v as Record<string, unknown>, key) : [key]
  })
}

describe('locale key parity', () => {
  const esKeys = flatKeys(es).sort()
  it('pt has exactly the same keys as es', () => {
    expect(flatKeys(pt).sort()).toEqual(esKeys)
  })
  it('en has exactly the same keys as es', () => {
    expect(flatKeys(en).sort()).toEqual(esKeys)
  })
})
```

- [ ] **Step 2: Run test to verify it passes**

Run: `bun run test -- src/lib/i18n/parity.test.ts`
Expected: PASS (2 tests). Si falla, corregir el JSON que tenga claves de más/menos (Task 3).

- [ ] **Step 3: Ensure `resolveJsonModule` / JSON import works**

Si el import de `.json` falla en vitest, agregar `assert: { type: 'json' }` no aplica; vitest soporta JSON import por defecto. Si tsc se queja, verificar `"resolveJsonModule": true` en `tsconfig.json` (ya suele estar). Run: `grep resolveJsonModule tsconfig.json` → si no está, agregarlo bajo `compilerOptions`.

- [ ] **Step 4: Commit**

```bash
git add src/lib/i18n/parity.test.ts
git commit -m "test(i18n): key parity across es/pt/en locales"
```

---

## Task 5: `tServer` — server-side translation (for PDF)

**Files:**
- Create: `src/lib/i18n/tServer.ts`
- Test: `src/lib/i18n/t.test.ts` (cubre `tServer`, la lógica de resolución es compartida)

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/i18n/t.test.ts
import { describe, it, expect } from 'vitest'
import { tServer } from './tServer'

describe('tServer', () => {
  it('returns the value for an existing key in the locale', () => {
    expect(tServer('en', 'invoicing.total')).toBe('Total')
    expect(tServer('pt', 'invoicing.newInvoice')).toBe('Nova fatura')
  })
  it('interpolates {vars}', () => {
    expect(tServer('es', 'billing.graceBanner', { date: '2026-08-01' }))
      .toBe('Tu suscripción está en período de gracia. Renueva antes de 2026-08-01.')
  })
  it('falls back to es when key missing in target locale', () => {
    // 'nonexistent.key' is in no locale → returns the key itself
    expect(tServer('en', 'nonexistent.key')).toBe('nonexistent.key')
  })
  it('returns the key when path does not resolve to a string', () => {
    expect(tServer('es', 'invoicing')).toBe('invoicing')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun run test -- src/lib/i18n/t.test.ts`
Expected: FAIL — cannot find module `./tServer`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/i18n/tServer.ts
import es from '../../../locales/es.json'
import pt from '../../../locales/pt.json'
import en from '../../../locales/en.json'
import type { Locale } from './normalizeLocale'

const DICTS: Record<Locale, Record<string, unknown>> = { es, pt, en }

function lookup(dict: Record<string, unknown>, key: string): string | undefined {
  let value: unknown = dict
  for (const k of key.split('.')) {
    if (value && typeof value === 'object' && k in (value as Record<string, unknown>)) {
      value = (value as Record<string, unknown>)[k]
    } else {
      return undefined
    }
  }
  return typeof value === 'string' ? value : undefined
}

export function tServer(locale: Locale, key: string, vars?: Record<string, string | number>): string {
  const raw = lookup(DICTS[locale], key) ?? lookup(DICTS.es, key) ?? key
  if (!vars) return raw
  return Object.entries(vars).reduce(
    (str, [k, v]) => str.replace(new RegExp(`\\{${k}\\}`, 'g'), String(v)),
    raw,
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun run test -- src/lib/i18n/t.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n/tServer.ts src/lib/i18n/t.test.ts
git commit -m "feat(i18n): tServer for server-side (PDF) translation with es fallback"
```

---

## Task 6: Rewrite `I18nProvider` (es/pt/en, 3 JSON, es fallback, persist via PATCH)

**Files:**
- Modify: `src/lib/i18n.tsx` (reescritura completa)

- [ ] **Step 1: Replace file contents**

```tsx
// src/lib/i18n.tsx
"use client";

import { createContext, useContext, useState, useEffect, ReactNode, useCallback } from "react";
import { normalizeLocale, DEFAULT_LOCALE, type Locale } from "./i18n/normalizeLocale";
import { useAuth } from "@/components/providers/AuthProvider";
import es from "../../locales/es.json";
import pt from "../../locales/pt.json";
import en from "../../locales/en.json";

type Dict = Record<string, unknown>;
const DICTS: Record<Locale, Dict> = { es, pt, en };

function lookup(dict: Dict, key: string): string | undefined {
  let value: unknown = dict;
  for (const k of key.split(".")) {
    if (value && typeof value === "object" && k in (value as Dict)) {
      value = (value as Dict)[k];
    } else {
      return undefined;
    }
  }
  return typeof value === "string" ? value : undefined;
}

interface I18nContextType {
  locale: Locale;
  setLocale: (locale: Locale) => void;
  t: (key: string, vars?: Record<string, string | number>) => string;
}

const I18nContext = createContext<I18nContextType | undefined>(undefined);

export function I18nProvider({ children }: { children: ReactNode }) {
  const { user } = useAuth();
  const [locale, setLocaleState] = useState<Locale>(DEFAULT_LOCALE);
  const [userTouched, setUserTouched] = useState(false);

  // Resolve initial locale. Priority: explicit user choice (localStorage) →
  // logged-in User.language → browser language → default (es).
  // Re-runs when `user` loads so a fresh device honors the saved profile language,
  // unless the visitor already picked a language this session (userTouched).
  useEffect(() => {
    if (userTouched) return;
    const saved = typeof window !== "undefined" ? localStorage.getItem("locale") : null;
    const nav = typeof navigator !== "undefined" ? navigator.language : null;
    const initial = normalizeLocale(saved ?? user?.language ?? nav);
    setLocaleState(initial);
    if (typeof document !== "undefined") document.documentElement.lang = initial;
  }, [user, userTouched]);

  const t = useCallback(
    (key: string, vars?: Record<string, string | number>): string => {
      const raw = lookup(DICTS[locale], key) ?? lookup(DICTS.es, key) ?? key;
      if (!vars) return raw;
      return Object.entries(vars).reduce(
        (str, [k, v]) => str.replace(new RegExp(`\\{${k}\\}`, "g"), String(v)),
        raw,
      );
    },
    [locale],
  );

  const setLocale = useCallback((next: Locale) => {
    setUserTouched(true);
    setLocaleState(next);
    if (typeof window !== "undefined") localStorage.setItem("locale", next);
    if (typeof document !== "undefined") document.documentElement.lang = next;
    // Persist to user profile (best-effort; ignore failure for logged-out users)
    fetch("/api/users/preferences", {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ language: next }),
    }).catch(() => {});
  }, []);

  return (
    <I18nContext.Provider value={{ locale, setLocale, t }}>
      {children}
    </I18nContext.Provider>
  );
}

export function useI18n() {
  const context = useContext(I18nContext);
  if (!context) throw new Error("useI18n must be used within an I18nProvider");
  return context;
}

export { LOCALES, type Locale } from "./i18n/normalizeLocale";
```

- [ ] **Step 2: Reorder `providers.tsx` — AuthProvider must wrap I18nProvider**

`I18nProvider` ahora llama `useAuth()`, así que debe estar **dentro** de `AuthProvider`. En
`src/app/providers.tsx` reemplazar el árbol de providers por:

```tsx
    <ThemeProvider
      attribute="class"
      defaultTheme="dark"
      enableSystem={false}
      disableTransitionOnChange
    >
      <AuthProvider>
        <I18nProvider>
          <BusinessProvider>
            <SubscriptionProvider>
              <ModulesProvider>
                <SubscriptionGate>{children}</SubscriptionGate>
              </ModulesProvider>
            </SubscriptionProvider>
          </BusinessProvider>
        </I18nProvider>
      </AuthProvider>
    </ThemeProvider>
```

(Se elimina `defaultLocale="pt"` y `AuthProvider` pasa a envolver a `I18nProvider`.) Verificar que
`useAuth()` expone `user?.language` (el tipo `user` de `AuthProvider` debe incluir `language`; si no,
agregarlo al tipo/select de sesión — reportar como concern si falta).

- [ ] **Step 3: Typecheck**

Run: `bun run build`
Expected: build OK. (Los consumidores existentes `useI18n().t` siguen funcionando; `locale` ahora es `'es'|'pt'|'en'`.)

- [ ] **Step 4: Commit**

```bash
git add src/lib/i18n.tsx src/app/providers.tsx
git commit -m "feat(i18n): rewrite I18nProvider (es/pt/en, static JSON, es fallback, PATCH persist)"
```

---

## Task 7: `User.language` default → `es` + preferences API accepts es/pt/en

**Files:**
- Modify: `prisma/schema.prisma:88`
- Modify: `src/app/api/users/preferences/route.ts`

- [ ] **Step 1: Change schema default**

En `prisma/schema.prisma`, línea del modelo User:
```prisma
  language  String   @default("pt-PT")
```
por:
```prisma
  language  String   @default("es")
```

- [ ] **Step 2: Push schema to DB**

Run: `bun run db:push`
Expected: `Your database is now in sync with your Prisma schema.` (default cambia; no borra datos existentes).

- [ ] **Step 3: Update preferences route to accept short codes + normalize legacy**

En `src/app/api/users/preferences/route.ts`, reemplazar el schema y el update:

```ts
import { normalizeLocale } from '@/lib/i18n/normalizeLocale';

const updatePreferencesSchema = z.object({
  // Accept short codes and legacy region codes; normalize below.
  language: z.string().optional(),
});
```

Y dentro del handler, reemplazar el bloque `data:` del `prisma.user.update`:

```ts
    const language = data.language ? normalizeLocale(data.language) : undefined;

    const user = await prisma.user.update({
      where: { id: session.user.id },
      data: {
        ...(language && { language }),
      },
      select: { id: true, name: true, email: true, language: true },
    });
```

- [ ] **Step 4: Typecheck**

Run: `bun run build`
Expected: build OK.

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma src/app/api/users/preferences/route.ts
git commit -m "feat(i18n): User.language default es + preferences API normalizes to es/pt/en"
```

---

## Task 8: `LanguageSelector` wired to `useI18n` (live change + persist)

**Files:**
- Modify: `src/components/layout/LanguageSelector.tsx`

- [ ] **Step 1: Replace file contents**

```tsx
// src/components/layout/LanguageSelector.tsx
"use client";

import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { Globe, Check } from "lucide-react";
import { useI18n, type Locale } from "@/lib/i18n";

const languages: { code: Locale; label: string; flag: string }[] = [
  { code: "es", label: "Español", flag: "🇪🇸" },
  { code: "pt", label: "Português", flag: "🇵🇹" },
  { code: "en", label: "English", flag: "🇬🇧" },
];

export function LanguageSelector() {
  const { locale, setLocale } = useI18n();

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button
          variant="ghost"
          size="icon"
          className="h-8 w-8 text-zinc-400 hover:text-white hover:bg-zinc-800"
        >
          <Globe className="h-4 w-4" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end" className="w-40 bg-zinc-900 border-zinc-800">
        {languages.map((lang) => (
          <DropdownMenuItem
            key={lang.code}
            onClick={() => setLocale(lang.code)}
            className="text-sm text-zinc-300 hover:text-white hover:bg-zinc-800 flex items-center justify-between"
          >
            <span>
              {lang.flag} {lang.label}
            </span>
            {locale === lang.code && <Check className="h-4 w-4 text-emerald-400" />}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `bun run build`
Expected: build OK.

- [ ] **Step 3: Manual smoke (documented, run after full plan)**

Tras `bun run build && bun run start` (puerto 3001): abrir la app, click en el globo → cambiar a English → los textos ya extraídos cambian en vivo; recargar → persiste.

- [ ] **Step 4: Commit**

```bash
git add src/components/layout/LanguageSelector.tsx
git commit -m "feat(i18n): LanguageSelector uses useI18n (es/pt/en live + persist)"
```

---

## Task 9: `formatDate` / `formatNumber` helpers (Intl)

**Files:**
- Create: `src/lib/i18n/format.ts`
- Test: `src/lib/i18n/format.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/i18n/format.test.ts
import { describe, it, expect } from 'vitest'
import { formatNumber, formatDate } from './format'

describe('formatNumber', () => {
  it('formats with locale grouping', () => {
    expect(formatNumber(1234.5, 'en')).toBe('1,234.5')
    expect(formatNumber(1234.5, 'es')).toBe('1.234,5')
  })
})

describe('formatDate', () => {
  it('formats an ISO date per locale', () => {
    const d = new Date('2026-08-01T00:00:00Z')
    // en → month/day/year style; just assert it contains the year and is a string
    expect(typeof formatDate(d, 'en')).toBe('string')
    expect(formatDate(d, 'en')).toContain('2026')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun run test -- src/lib/i18n/format.test.ts`
Expected: FAIL — cannot find module `./format`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/i18n/format.ts
import type { Locale } from './normalizeLocale'

const INTL_LOCALE: Record<Locale, string> = { es: 'es-ES', pt: 'pt-BR', en: 'en-US' }

export function formatNumber(n: number, locale: Locale): string {
  return new Intl.NumberFormat(INTL_LOCALE[locale]).format(n)
}

export function formatDate(date: Date, locale: Locale, opts?: Intl.DateTimeFormatOptions): string {
  return new Intl.DateTimeFormat(INTL_LOCALE[locale], opts ?? { timeZone: 'UTC' }).format(date)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun run test -- src/lib/i18n/format.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n/format.ts src/lib/i18n/format.test.ts
git commit -m "feat(i18n): formatNumber/formatDate Intl helpers per locale"
```

---

## Task 10: Translation script — `mergeMissing` (pure, TDD)

**Files:**
- Create: `scripts/i18n/mergeMissing.mjs`
- Test: `scripts/i18n/mergeMissing.test.mjs`
- Modify: `vitest.config.ts` (incluir `scripts/**/*.test.mjs`)

- [ ] **Step 1: Add test glob to vitest config**

En `vitest.config.ts`, cambiar `include`:
```ts
    include: ['src/**/*.test.ts', 'scripts/**/*.test.mjs'],
```

- [ ] **Step 2: Write the failing test**

```js
// scripts/i18n/mergeMissing.test.mjs
import { describe, it, expect } from 'vitest'
import { mergeMissing, missingKeys } from './mergeMissing.mjs'

describe('missingKeys', () => {
  it('lists dotted keys present in base but absent in target', () => {
    const base = { a: '1', b: { c: '2', d: '3' } }
    const target = { a: 'x', b: { c: 'y' } }
    expect(missingKeys(base, target)).toEqual(['b.d'])
  })
  it('returns [] when target has all base keys', () => {
    expect(missingKeys({ a: '1' }, { a: 'x' })).toEqual([])
  })
})

describe('mergeMissing', () => {
  it('fills missing keys from a translator without overwriting existing', () => {
    const base = { a: '1', b: { c: '2', d: '3' } }
    const target = { a: 'X', b: { c: 'Y' } }
    const translate = (key, value) => `${value}-tr`
    const merged = mergeMissing(base, target, translate)
    expect(merged).toEqual({ a: 'X', b: { c: 'Y', d: '3-tr' } })
  })
})
```

- [ ] **Step 3: Run test to verify it fails**

Run: `bun run test -- scripts/i18n/mergeMissing.test.mjs`
Expected: FAIL — cannot find module `./mergeMissing.mjs`.

- [ ] **Step 4: Write minimal implementation**

```js
// scripts/i18n/mergeMissing.mjs
export function missingKeys(base, target, prefix = '') {
  const out = []
  for (const [k, v] of Object.entries(base)) {
    const key = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object') {
      const sub = (target && typeof target === 'object' ? target[k] : undefined) ?? {}
      out.push(...missingKeys(v, sub, key))
    } else if (!target || !(k in target)) {
      out.push(key)
    }
  }
  return out
}

// translate(dottedKey, baseValue) => translatedString
export function mergeMissing(base, target, translate, prefix = '') {
  const result = Array.isArray(base) ? [] : { ...(target ?? {}) }
  for (const [k, v] of Object.entries(base)) {
    const key = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object') {
      result[k] = mergeMissing(v, (target && target[k]) ?? {}, translate, key)
    } else if (!target || !(k in target)) {
      result[k] = translate(key, v)
    }
  }
  return result
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `bun run test -- scripts/i18n/mergeMissing.test.mjs`
Expected: PASS (3 tests).

- [ ] **Step 6: Commit**

```bash
git add scripts/i18n/mergeMissing.mjs scripts/i18n/mergeMissing.test.mjs vitest.config.ts
git commit -m "feat(i18n): mergeMissing/missingKeys pure helpers for translate script"
```

---

## Task 11: Translation script — provider + CLI

**Files:**
- Create: `scripts/i18n/provider.mjs`
- Create: `scripts/i18n/translate.mjs`
- Create: `scripts/i18n/README.md`

- [ ] **Step 1: Write the provider (isolated, no key → identity + warn)**

```js
// scripts/i18n/provider.mjs
// Translation provider. Uses an LLM/DeepL-style HTTP API when TRANSLATION_API_KEY is set.
// Without a key it returns the source string unchanged and warns (script stays non-breaking).

const LANG_NAMES = { es: 'Spanish', pt: 'Portuguese', en: 'English' }

export function makeTranslator(targetLocale) {
  const key = process.env.TRANSLATION_API_KEY
  if (!key) {
    let warned = false
    return async (_dottedKey, value) => {
      if (!warned) {
        console.warn('[i18n] TRANSLATION_API_KEY not set — leaving source text as placeholder.')
        warned = true
      }
      return value
    }
  }

  // Anthropic Messages API (claude-haiku-4-5) — cheap, good for short UI strings.
  return async (_dottedKey, value) => {
    const res = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'x-api-key': key,
        'anthropic-version': '2023-06-01',
        'content-type': 'application/json',
      },
      body: JSON.stringify({
        model: 'claude-haiku-4-5-20251001',
        max_tokens: 256,
        messages: [{
          role: 'user',
          content: `Translate this UI string to ${LANG_NAMES[targetLocale]}. `
            + `Keep {placeholders} intact. Reply with ONLY the translation, no quotes:\n\n${value}`,
        }],
      }),
    })
    if (!res.ok) throw new Error(`translate API ${res.status}: ${await res.text()}`)
    const json = await res.json()
    return json.content?.[0]?.text?.trim() ?? value
  }
}
```

- [ ] **Step 2: Write the CLI**

```js
// scripts/i18n/translate.mjs
import { readFileSync, writeFileSync } from 'node:fs'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'
import { mergeMissing } from './mergeMissing.mjs'
import { makeTranslator } from './provider.mjs'

const __dirname = dirname(fileURLToPath(import.meta.url))
const LOCALES_DIR = join(__dirname, '../../locales')
const TARGETS = ['pt', 'en']
const force = process.argv.includes('--force')

const read = (l) => JSON.parse(readFileSync(join(LOCALES_DIR, `${l}.json`), 'utf8'))
const write = (l, data) => writeFileSync(join(LOCALES_DIR, `${l}.json`), JSON.stringify(data, null, 2) + '\n')

const base = read('es')

for (const locale of TARGETS) {
  const existing = force ? {} : read(locale)
  const translate = makeTranslator(locale)
  // mergeMissing is sync in signature but our translator is async — resolve sequentially.
  const merged = await mergeMissingAsync(base, existing, translate)
  write(locale, merged)
  console.log(`[i18n] ${locale}.json updated`)
}

// async variant that awaits the translator per key (keeps provider.mjs simple)
async function mergeMissingAsync(baseObj, target, translate, prefix = '') {
  const result = { ...(target ?? {}) }
  for (const [k, v] of Object.entries(baseObj)) {
    const key = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object') {
      result[k] = await mergeMissingAsync(v, (target && target[k]) ?? {}, translate, key)
    } else if (!target || !(k in target)) {
      result[k] = await translate(key, v)
    }
  }
  return result
}

// (mergeMissing imported for parity with the tested pure helper; async path used at runtime.)
void mergeMissing
```

- [ ] **Step 3: Write the README**

```markdown
# scripts/i18n — auto-traducción

`locales/es.json` es la fuente de verdad. Este script rellena las claves **faltantes**
en `pt.json` y `en.json` sin pisar las existentes.

## Uso

```bash
# solo faltantes (idempotente)
node scripts/i18n/translate.mjs

# re-traducir todo
node scripts/i18n/translate.mjs --force
```

## Proveedor

`provider.mjs` usa la API de Anthropic (`claude-haiku-4-5`) si `TRANSLATION_API_KEY`
está en el entorno. Sin key, deja el texto fuente como placeholder y avisa (no rompe).
Las traducciones se **commitean** — no se corre en CI todavía.
```

- [ ] **Step 4: Verify script runs without a key (non-breaking)**

Run: `node scripts/i18n/translate.mjs`
Expected: imprime el warning de `TRANSLATION_API_KEY` (una vez) + `pt.json updated` + `en.json updated`, sin cambios (ya están completos). Confirmar: `git diff --stat locales/` → sin cambios de contenido (o solo reformato consistente).

- [ ] **Step 5: Re-run parity test**

Run: `bun run test -- src/lib/i18n/parity.test.ts`
Expected: PASS (el script no rompió la paridad).

- [ ] **Step 6: Commit**

```bash
git add scripts/i18n/provider.mjs scripts/i18n/translate.mjs scripts/i18n/README.md
git commit -m "feat(i18n): translate.mjs CLI + isolated provider (Anthropic, non-breaking without key)"
```

---

## Task 12: Extract Billing screen + SubscriptionGate to `t()`

**Files:**
- Modify: `src/app/billing/page.tsx`
- Modify: `src/components/billing/SubscriptionGate.tsx`

- [ ] **Step 1: Read the two files to map hardcoded strings**

Run: `sed -n '1,80p' src/app/billing/page.tsx; echo ---; cat src/components/billing/SubscriptionGate.tsx`
Anota cada texto en español visible.

- [ ] **Step 2: Wire `useI18n` and replace strings (billing/page.tsx)**

Agregar al inicio del componente client:
```tsx
import { useI18n } from "@/lib/i18n";
// dentro del componente:
const { t } = useI18n();
```
Reemplazar los textos por claves ya definidas en Task 3 (`billing.*`, `common.*`). Mapa mínimo:
- Título "Mi suscripción" → `{t('billing.title')}`
- Botón "Pagar en USDT" → `{t('billing.payUsdt')}`
- Estado Activa/Gracia/Vencida → `{t('billing.statusActive')}` / `statusGrace` / `statusExpired`
- "Próximo pago" → `{t('billing.nextPayment')}`, "Monto mensual" → `{t('billing.amount')}`, "Plan" → `{t('billing.plan')}`

Si aparece un texto sin clave, **agregarlo a `locales/es.json`** (namespace `billing`) y correr Task 11 (`node scripts/i18n/translate.mjs`) para llenar pt/en, o traducir a mano las 2 entradas.

- [ ] **Step 3: Replace strings in SubscriptionGate.tsx**

Agregar `const { t } = useI18n();` y reemplazar los banners:
- Banner de gracia → `{t('billing.graceBanner', { date: <fecha formateada> })}`
- Banner vencido → `{t('billing.expiredBanner')}`

(Usar `formatDate(new Date(sub.currentPeriodEnd), locale)` de `@/lib/i18n/format` para la fecha; obtener `locale` de `useI18n()`.)

- [ ] **Step 4: Build + parity**

Run: `bun run build && bun run test -- src/lib/i18n/parity.test.ts`
Expected: build OK, parity PASS (si agregaste claves, deben existir en los 3 JSON).

- [ ] **Step 5: Commit**

```bash
git add src/app/billing/page.tsx src/components/billing/SubscriptionGate.tsx locales/*.json
git commit -m "feat(i18n): extract billing page + SubscriptionGate to t()"
```

---

## Task 13: Extract currency + rates settings to `t()`

**Files:**
- Modify: `src/app/warehouse/settings/currency/page.tsx`
- Modify: `src/app/warehouse/settings/rates/page.tsx`

- [ ] **Step 1: Read both files**

Run: `cat src/app/warehouse/settings/currency/page.tsx; echo ---; cat src/app/warehouse/settings/rates/page.tsx`

- [ ] **Step 2: Wire `useI18n` and replace (currency)**

`const { t } = useI18n();` y reemplazar: "Monedas"→`t('currency.title')`, "Moneda base"→`t('currency.base')`, "Moneda secundaria"→`t('currency.secondary')`, "Ninguna"→`t('currency.none')`, toast "guardado"→`t('currency.saved')`, "Guardar"→`t('common.save')`.

- [ ] **Step 3: Wire `useI18n` and replace (rates)**

Reemplazar: "Tasas de cambio"→`t('rates.title')`, "Manual"→`t('rates.manual')`, "Fuente"→`t('rates.source')`, "Valor"→`t('rates.value')`, toast→`t('rates.updated')`, "Guardar"→`t('common.save')`.

Cualquier texto extra → agregar a `es.json` (namespace correspondiente) + traducir pt/en.

- [ ] **Step 4: Build + parity**

Run: `bun run build && bun run test -- src/lib/i18n/parity.test.ts`
Expected: OK / PASS.

- [ ] **Step 5: Commit**

```bash
git add src/app/warehouse/settings/currency/page.tsx src/app/warehouse/settings/rates/page.tsx locales/*.json
git commit -m "feat(i18n): extract currency + rates settings to t()"
```

---

## Task 14: Extract modules (catalog + ModuleGate) to `t()`

**Files:**
- Modify: `src/components/modules/ModuleGate.tsx`
- Modify: `src/app/super-admin/modules/page.tsx`

- [ ] **Step 1: Read both files**

Run: `cat src/components/modules/ModuleGate.tsx; echo ---; sed -n '1,80p' src/app/super-admin/modules/page.tsx`

- [ ] **Step 2: Replace ModuleGate strings**

`const { t } = useI18n();` → "Módulo no activo"→`t('modules.notActive')`, descripción→`t('modules.notActiveDesc')`.

- [ ] **Step 3: Replace modules catalog strings**

"Catálogo de módulos"→`t('modules.catalog')`, "Complemento"→`t('modules.addon')`, + `common.*` (save/edit/create). Textos extra → `es.json` + traducir.

- [ ] **Step 4: Build + parity**

Run: `bun run build && bun run test -- src/lib/i18n/parity.test.ts`
Expected: OK / PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/modules/ModuleGate.tsx src/app/super-admin/modules/page.tsx locales/*.json
git commit -m "feat(i18n): extract modules catalog + ModuleGate to t()"
```

---

## Task 15: Extract invoicing (customers + sales/new) to `t()`

**Files:**
- Modify: `src/app/warehouse/customers/page.tsx`
- Modify: `src/app/sales/new/page.tsx`

- [ ] **Step 1: Read both files**

Run: `sed -n '1,80p' src/app/warehouse/customers/page.tsx; echo ---; sed -n '1,80p' src/app/sales/new/page.tsx`

- [ ] **Step 2: Replace strings**

`const { t } = useI18n();` en cada uno. Mapa: "Clientes"→`t('invoicing.customers')`, "Cliente"→`t('invoicing.customer')`, "Nueva factura"→`t('invoicing.newInvoice')`, "Subtotal"→`t('invoicing.subtotal')`, "Impuesto"→`t('invoicing.tax')`, "Total"→`t('invoicing.total')`, "Cant."→`t('invoicing.quantity')`, "Precio"→`t('invoicing.unitPrice')`, + `common.*`. Textos extra → `es.json` + traducir.

- [ ] **Step 3: Build + parity**

Run: `bun run build && bun run test -- src/lib/i18n/parity.test.ts`
Expected: OK / PASS.

- [ ] **Step 4: Commit**

```bash
git add src/app/warehouse/customers/page.tsx src/app/sales/new/page.tsx locales/*.json
git commit -m "feat(i18n): extract customers + new-sale screens to t()"
```

---

## Task 16: Localize the PDF receipt via `tServer`

**Files:**
- Modify: `src/lib/sales/saleData.ts` (agregar labels al DTO)
- Modify: `src/components/pdf/SaleDocumentPdf.tsx` (usar labels del DTO)
- Modify: `src/app/api/sales/[orderId]/pdf/route.ts` (resolver labels con `tServer` + locale del usuario)

- [ ] **Step 1: Read the DTO type**

Run: `cat src/lib/sales/saleData.ts`

- [ ] **Step 2: Add a `labels` field to `SaleDocumentData`**

En `src/lib/sales/saleData.ts`, agregar al type `SaleDocumentData`:

```ts
  labels: {
    voucher: string
    customer: string
    quantity: string
    unitPrice: string
    total: string
    description: string
    subtotal: string
    tax: string
  }
```

- [ ] **Step 3: Use `data.labels.*` in the PDF component**

En `src/components/pdf/SaleDocumentPdf.tsx`, reemplazar los literales:
- `COMPROBANTE` → `{data.labels.voucher}`
- `Cliente` → `{data.labels.customer}`
- `Descripción` → `{data.labels.description}`
- `Cant` → `{data.labels.quantity}`
- `Precio` → `{data.labels.unitPrice}`
- `Total` → `{data.labels.total}`
- (subtotal/tax donde aparezcan) → `{data.labels.subtotal}` / `{data.labels.tax}`

- [ ] **Step 4: Resolve labels in the endpoint via `tServer` + user locale**

En `src/app/api/sales/[orderId]/pdf/route.ts`:

```ts
import { tServer } from '@/lib/i18n/tServer'
import { normalizeLocale } from '@/lib/i18n/normalizeLocale'
```

Tras obtener la sesión, resolver el locale del usuario:
```ts
  const dbUser = session?.user?.id
    ? await prisma.user.findUnique({ where: { id: session.user.id }, select: { language: true } })
    : null
  const locale = normalizeLocale(dbUser?.language)
```

Y agregar al objeto `data`:
```ts
    labels: {
      voucher: tServer(locale, 'invoicing.voucher'),
      customer: tServer(locale, 'invoicing.customer'),
      quantity: tServer(locale, 'invoicing.quantity'),
      unitPrice: tServer(locale, 'invoicing.unitPrice'),
      total: tServer(locale, 'invoicing.total'),
      description: tServer(locale, 'common.description'),
      subtotal: tServer(locale, 'invoicing.subtotal'),
      tax: tServer(locale, 'invoicing.tax'),
    },
```

- [ ] **Step 5: Update the PDF render test if it constructs `SaleDocumentData`**

Run: `cat src/lib/pdf/renderSaleDocument.test.ts`
Si el test arma un `SaleDocumentData` literal, agregar el campo `labels` con strings de prueba para que compile. Run: `bun run test -- src/lib/pdf/renderSaleDocument.test.ts` → PASS.

- [ ] **Step 6: Build**

Run: `bun run build`
Expected: build OK.

- [ ] **Step 7: Commit**

```bash
git add src/lib/sales/saleData.ts src/components/pdf/SaleDocumentPdf.tsx "src/app/api/sales/[orderId]/pdf/route.ts" src/lib/pdf/renderSaleDocument.test.ts
git commit -m "feat(i18n): localize sale receipt PDF via tServer + user locale"
```

---

## Task 17: Extract business settings + login to `t()`

**Files:**
- Modify: `src/app/warehouse/settings/page.tsx`
- Modify: `src/app/(auth)/login/page.tsx`

- [ ] **Step 1: Read both files**

Run: `sed -n '1,80p' src/app/warehouse/settings/page.tsx; echo ---; sed -n '1,80p' 'src/app/(auth)/login/page.tsx'`

- [ ] **Step 2: Replace strings (settings)**

`const { t } = useI18n();` → "Marca"→`t('settings.branding')`, "Logo"→`t('settings.logo')`, "Cobrar impuesto"→`t('settings.taxEnabled')`, "Etiqueta del impuesto"→`t('settings.taxLabel')`, "Tasa del impuesto (%)"→`t('settings.taxRate')`, "Prefijo del comprobante"→`t('settings.docPrefix')`, toast→`t('settings.saved')`, "Guardar"→`t('common.save')`.

- [ ] **Step 3: Replace strings (login)**

"Iniciar sesión"→`t('auth.login')`, "Correo electrónico"→`t('auth.email')`, "Contraseña"→`t('auth.password')`, título→`t('auth.loginTitle')`, error→`t('auth.loginError')`. (Login es page client; ya monta el Provider — verificar que esté dentro de `<Providers>`; si login está fuera del provider, usar `tServer` no aplica en client → mover `LanguageSelector` al login con su propio `useI18n` requiere que login esté bajo el provider. Confirmar en Step 4.)

- [ ] **Step 4: Confirm login is under I18nProvider**

Run: `grep -rn "Providers" src/app/layout.tsx`
Expected: `<Providers>` envuelve `{children}` en el root layout → login queda cubierto. Si no, envolver login o su segmento en `<I18nProvider>`.

- [ ] **Step 5: Build + parity**

Run: `bun run build && bun run test -- src/lib/i18n/parity.test.ts`
Expected: OK / PASS.

- [ ] **Step 6: Commit**

```bash
git add src/app/warehouse/settings/page.tsx "src/app/(auth)/login/page.tsx" locales/*.json
git commit -m "feat(i18n): extract business settings + login to t()"
```

---

## Task 18: Full suite + build gate + manual smoke

**Files:** none (verification)

- [ ] **Step 1: Run the full test suite**

Run: `bun run test`
Expected: todos verdes (i18n nuevos + los 18 suites previos intactos).

- [ ] **Step 2: Production build**

Run: `bun run build`
Expected: build OK, sin type errors.

- [ ] **Step 3: Manual smoke (documented)**

`bun run start` (puerto 3001). Login → globo idioma → cambiar a **English** y **Português**: billing, monedas, módulos, facturación, ajustes y login cambian en vivo. Recargar → persiste (localStorage + `User.language`). Descargar un comprobante PDF con un usuario en `en` → labels en inglés.

- [ ] **Step 4: Final commit (si hubo ajustes de smoke)**

```bash
git add -A
git commit -m "chore(i18n): finalize block E — full suite green, build OK"
```

---

## Self-Review (contra el spec)

- **Un solo sistema i18n; duplicados eliminados** → Task 2 (borra `src/lib/i18n/*` muertos + `src/locales/`), Task 6 (único provider). ✅
- **Selector es/pt/en en vivo + persiste en User.language** → Task 8 (selector) + Task 6 (setLocale → PATCH) + Task 7 (API normaliza). ✅
- **Pantallas comerciales vía t()** → Tasks 12–17 (billing, monedas/tasas, módulos, facturación, ajustes, login). ✅
- **3 locales mismo set de claves (test paridad)** → Task 4. ✅
- **translate.mjs llena faltantes sin pisar** → Tasks 10 (mergeMissing TDD) + 11 (CLI + provider). ✅
- **PDF en idioma correspondiente** → Task 16 (tServer + labels DTO + locale del usuario). ✅
- **t() + paridad testeados** → Tasks 4, 5 (t/tServer), 9 (format), 10 (mergeMissing). ✅
- **User.language legacy pt-PT normalizado** → Task 1 (normalizeLocale) usado en provider/API/PDF. ✅
- **Formato fechas/números por locale (Intl)** → Task 9. ✅

**Nota de decisión:** el spec menciona `PUT /api/users/preferences`; el endpoint real es `PATCH` — el plan usa `PATCH` (lo existente) para no romper el contrato actual.
